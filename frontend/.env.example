# Backend API URL
# Local development (Vite proxy handles /api/* — so this value is optional locally)
VITE_API_URL=http://localhost:5000

# Production (set to your Render backend URL)
# VITE_API_URL=https://fundsroom-erp-api.onrender.com
